// v3.0 — stub (Vue version of useScheduledTaskDesktopNotifications)
import { onMounted, onUnmounted } from 'vue'
export function useScheduledTaskDesktopNotifications() {
  onMounted(() => {})
  onUnmounted(() => {})
}
